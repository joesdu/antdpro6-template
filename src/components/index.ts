import { AvatarDropdown, AvatarName } from './RightContent/AvatarDropdown';
import { Question, SelectLang } from './RightContent';

/**
 * 这个文件作为组件的目录
 * 目的是统一管理对外输出的组件，方便分类
 */
/**
 * 布局组件
 */
import IconFont from './IconFont';

export { AvatarDropdown, IconFont, Question, SelectLang, AvatarName };
